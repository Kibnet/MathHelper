# 🚀 Как запускать Expression Editor

## Версии приложения

В проекте теперь есть **две версии**:

### 1️⃣ Оригинальная версия (монолитная)
📄 **Файл:** `expression-editor.html`
- Весь код в одном HTML файле
- Работает автономно
- Подходит для быстрых правок

**Запуск:**
```
http://localhost:8000/expression-editor.html
```

---

### 2️⃣ Модульная версия (рекомендуется) ⭐
📄 **Файл:** `expression-editor-modular.html`
- Использует ES6 модули из папки `js/`
- Чистая архитектура (логика отделена от UI)
- Легко тестируется
- Легко поддерживается

**Запуск:**
```
http://localhost:8000/expression-editor-modular.html
```

---

## 🧪 Тесты

📄 **Файл:** `tests/test-runner.html`

**Запуск тестов:**
```
http://localhost:8000/tests/test-runner.html
```

**Что тестируется:**
- ✅ Парсинг выражений (23 теста)
- ✅ Приоритет операторов
- ✅ Обработка скобок
- ✅ Обработка ошибок

---

## 📋 Пошаговая инструкция

### Шаг 1: Запустить локальный сервер

Если сервер еще не запущен:

```bash
# В командной строке
cd "c:\Projects\My\MathHelper"
python -m http.server 8000
```

Должно появиться:
```
Serving HTTP on :: port 8000 (http://[::]:8000/) ...
```

### Шаг 2: Открыть в браузере

Выберите нужную версию:

**Модульная версия (новая):**
```
http://localhost:8000/expression-editor-modular.html
```

**Оригинальная версия:**
```
http://localhost:8000/expression-editor.html
```

**Тесты:**
```
http://localhost:8000/tests/test-runner.html
```

---

## 🔍 Как это работает (модульная версия)

### Структура импортов в HTML:

```html
<script type="module">
  // Импорт модулей
  import { ExpressionParser } from './js/core/parser.js';
  import { findAllSubexpressions } from './js/core/analyzer.js';
  import { getApplicableRules } from './js/core/rules.js';
  import { expressionToString } from './js/utils/helpers.js';
  
  // Использование
  const parser = new ExpressionParser('2 + 3');
  const ast = parser.parse();
  const rules = getApplicableRules(ast);
</script>
```

### Загружаемые модули:

```
js/core/parser.js       → Парсинг выражений
js/core/analyzer.js     → Поиск подвыражений, расчет рамок
js/core/rules.js        → Правила трансформаций
js/utils/helpers.js     → Утилиты для работы с AST
```

---

## ⚡ Быстрый старт

### Вариант А: Через PowerShell

```powershell
cd "c:\Projects\My\MathHelper"

# Запустить сервер (если не запущен)
python -m http.server 8000

# В новом окне PowerShell - открыть модульную версию
start http://localhost:8000/expression-editor-modular.html

# Или открыть тесты
start http://localhost:8000/tests/test-runner.html
```

### Вариант Б: Через браузер напрямую

1. Убедитесь что сервер запущен (`python -m http.server 8000`)
2. Откройте в браузере: http://localhost:8000
3. Выберите нужный файл:
   - `expression-editor-modular.html` - новая версия
   - `expression-editor.html` - старая версия
   - `tests/test-runner.html` - тесты

---

## 🎯 Функциональность

### Модульная версия включает:

✅ **Ввод выражений**
- Текстовое поле для ввода
- Кнопки Build/Clear
- Валидация синтаксиса

✅ **Визуализация**
- Отображение выражения моноширинным шрифтом
- Цветные рамки для всех подвыражений
- Многоуровневая раскладка (без перекрытий)

✅ **Интерактивность**
- Наведение на рамку → подсветка текста
- Клик на рамку → показ доступных команд
- Группировка команд по категориям

✅ **Трансформации**
- Вычисления (2+3 → 5)
- Упрощения (x*1 → x)
- Раскрытия (a*(b+c) → a*b+a*c)
- Перестановки (a+b → b+a)
- Обёртки (a → (a), a → a*1)

---

## 🐛 Решение проблем

### Проблема: "Module not found"

**Причина:** Модули не загружаются через `file://` протокол

**Решение:** Убедитесь что:
1. Локальный сервер запущен (`python -m http.server 8000`)
2. Используете `http://localhost:8000/...`, а не `file:///...`

---

### Проблема: "Port already in use"

**Причина:** Порт 8000 уже занят

**Решение:** Используйте другой порт:
```bash
python -m http.server 8080
# Затем откройте http://localhost:8080/...
```

---

### Проблема: Рамки не отображаются

**Причина:** Введено невалидное выражение

**Решение:** 
1. Проверьте синтаксис выражения
2. Смотрите сообщения об ошибках в красной плашке
3. Примеры валидных выражений:
   - `2 + 3`
   - `a * (b + c)`
   - `x - y / 2`

---

## 📚 Примеры выражений

Попробуйте ввести:

```
2 + 3 * 4           # Приоритет операторов
(2 + 3) * 4         # Скобки
a + 2 + 3           # Перекрывающиеся подвыражения
x * 1               # Упрощение
2 * (a + b)         # Раскрытие скобок
--5                 # Двойное отрицание
```

---

## 🔗 Полезные ссылки

- **Документация модулей:** `README-MODULES.md`
- **Исходный код модулей:** папка `js/`
- **Тесты:** папка `tests/`
- **Старая версия:** `expression-editor.html` (для сравнения)

---

## 💡 Совет

Используйте **модульную версию** для разработки и тестирования:
- Проще дебажить (модули в отдельных файлах)
- Можно тестировать логику изолированно
- Легче добавлять новые функции
- Чище структура кода

Старую версию можно оставить как fallback или для пользователей без сервера.
